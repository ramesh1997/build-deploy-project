name: Node Continuous integration
on: 
  workflow_dispatch:
  
jobs: 
  first-job: 
    runs-on: ubuntu-latest
    steps: 
      - run: node --version
        name: Check Node verion
      - name: create txt file
        run: |
          echo "hello world" >> "${{github_workplace }}/data.txt"
          ls -la
          cat "${{github_workplace }}/data.txt"

  second-job:
    runs-on: ubuntu-latest
    needs: first-job
    steps: 
      - run: |
          ls -la
          cat "${{github_workplace }}/data.txt"
        name: print the data 
    
    
      
